apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "consumer-service.fullname" . }}-config
  labels:
    app: {{ include "consumer-service.name" . }}
data:
  # Add environment variables or config here
