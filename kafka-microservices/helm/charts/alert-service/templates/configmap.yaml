apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "alert-service.fullname" . }}-config
  labels:
    app: {{ include "alert-service.name" . }}
data:
  # Add environment variables or config here
