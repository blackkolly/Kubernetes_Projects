apiVersion: v1
kind: Service
metadata:
  name: {{ include "producer-service.fullname" . }}
  labels:
    app: {{ include "producer-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "producer-service.name" . }}
    release: {{ .Release.Name }}
